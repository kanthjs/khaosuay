% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cook_crd.R
\name{cook_crd}
\alias{cook_crd}
\title{หุงข้าว CRD — Completely Randomized Design}
\usage{
cook_crd(
  data,
  response,
  treatment = "treatment",
  factors = NULL,
  tasted = NULL,
  posthoc = c("tukey", "lsd", "duncan"),
  alpha = 0.05,
  verbose = TRUE
)
}
\arguments{
\item{data}{washed_rice object หรือ data.frame}

\item{response}{character ชื่อ response variable (ระบุหลายตัวได้)}

\item{treatment}{character ชื่อคอลัมน์ treatment (single factor)}

\item{factors}{character vector ชื่อปัจจัย (สำหรับ factorial เช่น c("variety", "fertilizer"))}

\item{tasted}{tasted_rice object จาก taste_rice() — ถ้ามีจะไม่เช็คซ้ำ}

\item{posthoc}{character วิธี post-hoc: "tukey" (default), "lsd", "duncan"}

\item{alpha}{numeric ระดับนัยสำคัญ (default = 0.05)}

\item{verbose}{logical แสดงรายงาน (default = TRUE)}
}
\value{
object class "cooked_rice" (list) ประกอบด้วย:
  \item{results}{list ผลวิเคราะห์แยกรายตัวแปร}
  \item{design}{"CRD"}
  \item{data}{data.frame ที่ใช้วิเคราะห์}
  \item{settings}{list ค่าที่ตั้ง}
}
\description{
วิเคราะห์สถิติแปลงทดลองแบบ CRD รองรับทั้ง Single Factor และ Factorial
รับผลจาก taste_rice() เพื่อเลือก Parametric / Non-parametric อัตโนมัติ
}
\examples{
\dontrun{
# Single factor
washed <- wash_rice(my_data)
tasted <- taste_rice(washed, response = "yield", mode = "model")
cooked <- cook_crd(washed, response = "yield", tasted = tasted)

# Factorial
cooked <- cook_crd(washed, response = "yield",
                   factors = c("variety", "fertilizer"),
                   tasted = tasted)

# ดูผล
cooked$results$yield$summary_table
cooked$results$yield$group_letters
}
}
