% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cook_rcbd.R
\name{cook_rcbd}
\alias{cook_rcbd}
\title{หุงข้าว RCBD — Randomized Complete Block Design}
\usage{
cook_rcbd(
  data,
  response,
  treatment = "treatment",
  block = "rep",
  factors = NULL,
  tasted = NULL,
  posthoc = c("tukey", "lsd", "duncan"),
  alpha = 0.05,
  verbose = TRUE
)
}
\arguments{
\item{data}{washed_rice object หรือ data.frame}

\item{response}{character ชื่อ response variable (ระบุหลายตัวได้)}

\item{treatment}{character ชื่อคอลัมน์ treatment (single factor)}

\item{block}{character ชื่อคอลัมน์ block/rep (default = "rep")}

\item{factors}{character vector ชื่อปัจจัย (สำหรับ factorial in RCBD)}

\item{tasted}{tasted_rice object จาก taste_rice() — ถ้ามีจะไม่เช็คซ้ำ}

\item{posthoc}{character วิธี post-hoc: "tukey" (default), "lsd", "duncan"}

\item{alpha}{numeric ระดับนัยสำคัญ (default = 0.05)}

\item{verbose}{logical แสดงรายงาน (default = TRUE)}
}
\value{
object class "cooked_rice" (list) ประกอบด้วย:
  \item{results}{list ผลวิเคราะห์แยกรายตัวแปร}
  \item{design}{"RCBD"}
  \item{data}{data.frame ที่ใช้วิเคราะห์}
  \item{settings}{list ค่าที่ตั้ง}
}
\description{
วิเคราะห์สถิติแปลงทดลองแบบ RCBD รองรับ Single Factor และ Factorial in RCBD
รับผลจาก taste_rice() เพื่อเลือก Parametric / Non-parametric อัตโนมัติ
}
\examples{
\dontrun{
# Single factor RCBD
washed <- wash_rice(my_data, design_check = TRUE)
tasted <- taste_rice(washed, response = "yield", block = "rep")
cooked <- cook_rcbd(washed, response = "yield", block = "rep",
                    tasted = tasted)

# Factorial in RCBD
cooked <- cook_rcbd(washed, response = "yield",
                    factors = c("variety", "chemical"),
                    block = "rep", tasted = tasted)

# ดูผล
cooked$results$yield$summary_table
cooked$results$yield$cv_percent
}
}
