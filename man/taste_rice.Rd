% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taste_rice.R
\name{taste_rice}
\alias{taste_rice}
\title{ชิมข้าว — ตรวจสอบ Assumptions ก่อนวิเคราะห์สถิติ (taste_rice) v2.0}
\usage{
taste_rice(
  data,
  response,
  treatment = "treatment",
  block = NULL,
  mode = c("both", "quick", "model"),
  alpha = 0.05,
  plot = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data}{washed_rice object หรือ data.frame}

\item{response}{character ชื่อคอลัมน์ response (ระบุหลายตัวได้)}

\item{treatment}{character ชื่อคอลัมน์ treatment (ใส่ vector ได้ เช่น c("A", "B") สำหรับ Factorial)}

\item{block}{character ชื่อคอลัมน์ block สำหรับ RCBD (default = NULL = CRD)}

\item{mode}{"quick" = เช็คจาก raw data, "model" = เช็คจาก residuals, "both" = ทำทั้งสองแบบ (default)}

\item{alpha}{ระดับนัยสำคัญ (default = 0.05)}

\item{plot}{logical แสดง diagnostic plots (default = TRUE)}

\item{verbose}{logical แสดงรายงาน (default = TRUE)}
}
\value{
object class "tasted_rice"
}
\description{
รองรับ CRD, RCBD และ Factorial Design
}
